# Task 7.3 ZAP Consumer (DAST) — Implementation Plan

> **For Claude:** This plan follows the Task 7.5b phased implementation precedent (Phase 0 verification → Phase 1 framework primitives → Phase 2 lifecycle integration → Phase 3 testing infrastructure → Phase 4 atomic commit). Single atomic commit at Phase 4 covers all phases. Deviation discipline + phase-by-phase Copy-Paste Reply instructions under the project's standing rule.

**Goal:** Ship ZAP as first DockerServiceRunner consumer (per Task 7.5b framework; commit 1306ca8). Validates DAST capability for ShieldScan v1; fills SPEC §7.1 wire-schema auth block gap (cookie type v1); enables Quick + Full pricing tier scans (Mode B + Mode C).

**Architecture:** Per design doc `plans/2026-05-09-task-7.3-zap-consumer-design.md` (commit 682cfcc). 9 brainstorming decisions Q1–Q9 + 18 Phase 0 V-items + 5 Phase 0 deviations resolved. Path Y verification grounded Q5+Q6+Q7+Q8 against actual ZAP API + Nmap precedent + SPEC §7.1 wire-schema pre-pin.

**Tech Stack:** Go 1.26.2; Docker SDK v28.5.2+incompatible (already imported via Task 7.5a/b); zerolog; net/http stdlib (per Task 7.5b custom HTTP client framework); testify v1.11.1; existing service framework at `internal/tools/docker/service/` (Task 7.5b).

**Reference artifacts:**
- Design doc: `plans/2026-05-09-task-7.3-zap-consumer-design.md` (commit 682cfcc in shieldscan-docs)
- Framework: shieldscan-engine commit 1306ca8 (Task 7.5b DockerServiceRunner + ContainerFactory hook)
- Symmetric pattern precedent: shieldscan-engine commit 872b2b0 (Task 7.2 Nmap consumer) + shieldscan-engine commit 1306ca8 (Task 7.5b service framework + tests)
- ADR-026: SPECIFICATION.md §13 in shieldscan-docs (canonical DockerServiceRunner architecture + ContainerFactory addendum)

---

## Phase 0 — Empirical Verification (READ-WRITE WITH CONTAINER; ~60-90min; NO COMMIT)

ZAP-container-empirical verification of 18 V-items surfaced by design doc Path Y verification rounds. Some V-items are documentation-only; most require ZAP container spinup at pinned digest `sha256:8770b...` for empirical confirmation.

### Phase 0 Status

**Phase 0 Status (2026-05-09):** PENDING. Phase 0 of Task 7.5b precedent established empirical verification before Phase 1 (caught V8 brainstorming-misframing + V2 WarmPool extensibility + V4 ZAP cleanup unclear + V5 MobSF model mismatch). Task 7.3 Phase 0 follows same discipline.

### V1–V18 Verification Items (per design doc §3.5)

- **V1.** Verify pinned ZAP digest resolves correctly + multi-arch. Run: `docker pull ghcr.io/zaproxy/zaproxy@sha256:8770b23f9e8b49038f413cb2b10c58c901e5b6717be221a22b1bcab5c9771b8a`; `docker manifest inspect`; verify amd64 + arm64.
- **V2.** Verify ZAP container API-key default behavior. Spin up: `docker run --rm -p 8080:8080 ghcr.io/zaproxy/zaproxy:stable@sha256:8770b... zap.sh -daemon -host 0.0.0.0 -port 8080`. Inspect logs for auto-generated key OR test `ZAP_API_KEY` env var override. Document exact key resolution mechanism.
- **V3.** Empirically test `WithAPIKeyHeader("X-ZAP-API-Key", key)` backward-compat. Audit-only — confirms whether undocumented header path works alongside canonical query-param. Outcome documented; doesn't change Q7(B) lock.
- **V4.** Verify Attack Strength + Alert Threshold enum exact strings via `/JSON/ascan/view/scanners/?apikey=`. Confirm `DEFAULT|LOW|MEDIUM|HIGH|INSANE` for strength; `LOW|MEDIUM|HIGH|OFF|DEFAULT` for threshold. Pin exact strings for consumer code.
- **V5.** Verify Default Policy exact string via `/JSON/ascan/view/scanPolicyNames/?apikey=`. Confirm `"Default Policy"` (whitespace + capitalization) for ExtraArgs allowlist.
- **V6.** Verify CWE field present in `/JSON/core/view/alert/?id=N` response + integer type. Sample alert; confirm `cweid` field presence + type.
- **V7.** Verify `/JSON/core/view/alerts/` default count cap. Trigger many alerts (juice-shop testbed); fetch with `start=0&count=5000`; observe whether response truncated. Document cap.
- **V8.** Verify Severity casing convention with shieldscan-api. Read shieldscan-api Pydantic + SQLAlchemy schemas: `grep "severity\|Severity"` in shieldscan-api models. Confirm pass-through "High"/"Medium"/"Low"/"Informational" vs lowercase normalize.
- **V9.** Decide False Positive risk filtering policy. Test scenarios; document decision (parser-filter vs surface as severity value).
- **V10.** Decide evidence placement. Inspect actual ZAP `evidence` field content; decide Request vs Response vs Metadata `evidence`.
- **V11.** Decide solution placement. Sample alert `solution` field; decide Metadata `solution` vs Description-append.
- **V12.** Decide tags shape. Sample `alertTags` object; decide flatten to `RawFinding.Tags` vs JSON-string in Metadata.
- **V13.** Verify `formBasedAuthentication` `authMethodConfigParams` URL-encoded format. Empirical test: configure form-based auth via `/JSON/authentication/action/setAuthenticationMethod/`. Document exact URL-encoded format for ExtraArgs `zap.auth.*` escape-hatch path.
- **V14.** Verify `userId` parameter on `/JSON/spider/action/scan/` + `/JSON/ascan/action/scan/` for authenticated scanning. Empirically confirm parameter recognized + behavior.
- **V15.** Verify session re-auth behavior on logged-out-indicator regex match. Trigger logged-out condition mid-scan; observe ZAP behavior.
- **V16.** Cross-walk ZAP alert IDs ↔ CWE IDs. Sample 10–20 alerts; document `cwe` field reliability + alert-ID-without-CWE fallback.
- **V17.** Verify default policy + 9 built-in policies exact names. Run `/JSON/ascan/view/scanPolicyNames/?apikey=` against pinned digest. Pin exact strings: `"Default Policy"`, `"Penetration Tester Policy"`, `"API Policy"`, `"Developer CI/CD Policy"`, `"Developer Standard Policy"`, `"Developer Full Policy"`, `"QA CI/CD Policy"`, `"QA Standard Policy"`, `"QA Full Policy"`.
- **V18.** Verify `/JSON/httpSessions/` cookie injection mechanism. Configure target context; inject cookie via `/JSON/httpSessions/action/createEmptySession/` + `/JSON/httpSessions/action/setSessionTokenValue/`. Confirm cookies pass through to subsequent spider/ascan requests.

### Phase 0 STOP GATE

- **V8** (Severity casing) is decision-territory; cross-repo coordination dependency. If shieldscan-api convention differs from ZAP pass-through, surface for direction before Phase 1 — affects parser code + consumer-side tests.
- **V13** (formBasedAuthentication URL-encoded format) is decision-territory; if format too complex for ExtraArgs escape-hatch (>500 chars or unsafe-encoded chars), flip Q6 lock to Option α (defer auth entirely v1) for direction.
- Other V-items are factual/mechanical; auto-correct + document in Phase 4 commit.

Phase 0 produces NO commit. Surface report enables Phase 1+ design grounding.

### Phase 0 Cross-Repo Verification (V8)

```bash
cd /home/mahmoud/workspace/shieldscan-api
grep -rn "severity\|Severity" --include="*.py" . | grep -i "model\|schema\|enum" | head -20
```

Document cross-repo Severity convention; surface findings before Phase 1 ZAP parser implementation.

---

## Phase 1 — ZAP Consumer Subpackage (~2-3h; NO COMMIT)

### Files Created

Files created/modified in Phase 1 (per design doc §4):

**ZAP consumer subpackage** (V8 lock — net-new at `internal/tools/docker/service/zap/`):
- `zap.go` — Config struct + ScanMode enum + NewBuildScan constructor + entry point; ~150 LoC
- `auth.go` — `zapQueryParamAuth` AuthFunc closure + cookie injection helper + ExtraArgs `zap.auth.*` parsing + form-based escape-hatch path; ~120 LoC
- `spider.go` — Mode B + Mode C spider orchestration + PollUntil integration + RFC1918 URL filtering; ~140 LoC
- `ascan.go` — Mode C active scan orchestration + `scanPolicyName` allowlist validation + PollUntil integration + scan stop on timeout; ~150 LoC
- `parser.go` — alert parsing + Q8 mapping table per design doc §3.4 + Metadata key population + Severity casing per V8 verification; ~220 LoC
- `target.go` — target validation: scheme required + AllowPrivateTargets RFC1918 + link-local enforcement; ~70 LoC

**Test files:**
- `zap_test.go` (~150 LoC)
- `auth_test.go` (~150 LoC)
- `spider_test.go` (~120 LoC)
- `ascan_test.go` (~150 LoC)
- `parser_test.go` (~250 LoC; Q8 mapping correctness)
- `target_test.go` (~80 LoC)
- `testhelpers_test.go` (~150 LoC; stub ZAP responder using `httptest.NewServer`; canned alert + spider + ascan responses)

**Cross-package modifications** (Q3 lock + Q6 lock):

- **Q3 docker-compose modifications:**
  - `deploy/docker-compose.services.yml` — remove ZAP service entry (~10 LoC removed)
  - `deploy/docker_compose_test.go` — drop ZAP service assertions at lines 48 + 68 (~10 LoC removed)
- **Q6 ScanConfig.AuthConfig field addition:**
  - `internal/tools/runner.go` — add `AuthConfig` pointer field to ScanConfig struct + AuthConfig type definition (`Type string`, `Data string`)
  - JSON unmarshaling test in `runner_test.go` (~30 LoC additive)

**Total estimated:** ~750 LoC production + ~1050 LoC tests + 20 LoC cross-package removals + 30 LoC cross-package additions. Net delta: ~+1830 LoC engine-side.

### Phase 1 Quality Gate

- gofmt clean
- go vet clean
- golangci-lint clean
- all Phase 1 tests pass under `go test -race -count=1 ./...`
- existing `internal/tools/docker/service` tests still pass (Task 7.5b backward-compat)
- existing 22 packages green
- new `internal/tools/docker/service/zap` package green
- `deploy/docker_compose_test.go` test set updated coherently

**PAUSE AFTER PHASE 1.**

---

## Phase 2 — Cross-Repo Coordination Decision (~30-45min; READ-ONLY THEN COMMIT IN shieldscan-api OR FORWARD-PIN; NO ENGINE COMMIT)

Q6 lock requires shieldscan-api Pydantic AuthConfig model + SQLAlchemy `ScanJob.auth_config` column coordination. Per ADR-024 + ADR-025 cross-repo coordination precedent, options:

- **Option (a) — Single coordinated commit:** Phase 2 lands shieldscan-api Pydantic + SQLAlchemy changes in shieldscan-api repo before Phase 4 atomic engine commit. Coordinated cross-repo state.
- **Option (b) — Forward-pin to separate task:** Phase 4 engine commit acknowledges cross-repo gap; separate Task 7.3.x followup task lands shieldscan-api alignment.

**Recommended Option (a)** — coordinated commit. shieldscan-api changes are bounded (~50–100 LoC: Pydantic model + SQLAlchemy column + test); coupling Phase 4 engine commit to coordinated shieldscan-api state preserves single-cycle integrity. Option (b) is fallback if shieldscan-api implementation surfaces unexpected complexity (e.g., Fernet encryption integration questions).

**PAUSE AFTER PHASE 2.** Surface direction lock + applied changes; do not proceed to Phase 3 without coordination state confirmed.

---

## Phase 3 — Testing Infrastructure + Documentation (~45-60min; NO COMMIT)

Coverage report for `service/zap` subpackage (target ≥80% per Task 7.5b precedent); race detector clean; godoc verification for new exported types (`Config`, `ScanMode`, `NewBuildScan`, `AuthConfig`). Package-level documentation in `zap.go` top-of-file; consumer-integration docstring on Config struct mirroring Task 7.5b `service.go` pattern (commit 1306ca8).

### Phase 3 Quality Gate

- Coverage ≥80% on `service/zap` package
- race detector clean
- godoc renders correctly
- lint clean

**PAUSE AFTER PHASE 3.**

---

## Phase 4 — DRIFT-LOG + Atomic Commit (~30-45min; SINGLE COMMIT)

Land all Phase 1+3 work in single atomic commit per Task 7.5a + Task 7.5b precedent.

### Pre-Commit Steps

1. Update `DRIFT-LOG.md` with Phase 1+3 deviation enumeration following `## YYYY-MM-DD — Task X.Y` format precedent (Task 7.5b 1306ca8 commit pattern).
2. Final quality gate: gofmt + go vet + golangci-lint + `go test -race -count=1 ./...`.
3. Verify staged file set: ZAP subpackage (production + test) + cross-package modifications (docker-compose + ScanConfig + worker integration if needed) + DRIFT-LOG + commit message.
4. Coordinate with shieldscan-api state per Phase 2 decision.

### Commit Message Shape

Mirroring 1306ca8 + 872b2b0 + f5d77c8 commit message conventions; documents architecture per design doc 682cfcc; enumerates V1–V18 + 5 Phase-0 deviations with disposition; forward-pins Task 7.5c V4 verification + Task 7.5b documentation cleanup + v2 typed-enum AuthConfig migration.

**PAUSE AFTER PHASE 4.** Task 7.3 engine-side closed.

---

## Forward-Pins After Task 7.3 Closes

- **Task 7.5c V4 verification execution** — if `newSession` verified COMPLETE, flip `cfg.EphemeralContainer` to `false` + design `CleanupFunc`; restores warm-pool startup amortization for ZAP. Phase D of Task 7.5c executes the artifact revision.
- **Task 7.5b documentation cleanup** — design doc 3067c92 + ADR-026 addendum 066c81f reference ZAP using `WithAPIKeyHeader` (incorrect per Q7 verification). Phase 5 Task 7.3 docs followup OR separate Task 7.5b.x cleanup task.
- **Task 7.3 Phase 5 docs followup** (mirrors Task 7.5b Phase 5 pattern): design doc post-implementation alignment; SPEC §3.2 architecture diagram update (`zap.go` path → `docker/service/zap/`); ADR-006 risk #14 ZAP-specific notes if any; potentially ADR-028 for AuthConfig wire-schema enablement (per ADR-027 schema-extension precedent — affects shieldscan-api Pydantic + SQLAlchemy + downstream ADR-015 forcing function status update).
- **v2 typed enum expansion for `AuthConfig.Type`** (`form_based`, `bearer`, `basic`) — when Task 7.3 execution surfaces empirical form-based config formats AND/OR 2nd auth-supporting consumer surfaces.
- **Mode D (Spider + AJAX + Active) v2 implementation** — when SPA customer demand surfaces.
- **`WithAPIKeyQueryParam` framework helper promotion** (Phase 5.D Task 7.5b precedent threshold gate) — when 2nd query-param-auth tool surfaces.
- **Pagination upgrade** (Q8 Option ii) — if execution surfaces alert count exceeding default cap.
- **Task 7.4 (MobSF)** — second DockerServiceRunner consumer; warm-pool path with md5-tracked `CleanupFunc`.
- **Task 7.1 (Trivy) + Task 7.6 (SQLMap)** — DockerRunner exec-shape consumers; per-Task-7.2 precedent.

---

## Discipline Pattern Carry-Forward

The verify-then-adapt-then-document discipline established by Task 7.5a + Task 7.2 + Task 7.5b carries forward. Anticipated deviation surfaces:

- Phase 0 V1–V18 will surface 5–10 factual deviations (auto-corrected)
- Phase 1 ZAP parser may surface alert-field-shape edge cases (`cweid` type drift; tags object handling; reference array nullability)
- Phase 2 cross-repo Pydantic/SQLAlchemy may surface Fernet encryption integration complexity

Pre-implementation guidance is unreliable specifically because edge-case interleavings + cross-repo integration surface during implementation; the discipline pattern catches them transparently. Phase 4 commit message footnote enumerates deviations explicitly.

---

## Cross-References

- Design doc: `plans/2026-05-09-task-7.3-zap-consumer-design.md` (commit 682cfcc in shieldscan-docs)
- Framework foundation: shieldscan-engine commit 1306ca8 (Task 7.5b DockerServiceRunner + ContainerFactory hook)
- Consumer pattern precedent: shieldscan-engine commit 872b2b0 (Task 7.2 Nmap consumer)
- Brainstorming chain artifact: in conversation memory + design doc 682cfcc §2 + §3.5
- SPECIFICATION.md §13 ADR-008 (MobSF DockerServiceRunner consumer precedent — non-binding for ZAP)
- SPECIFICATION.md §13 ADR-015 (decrypted credentials in Redis transit; forcing function for auth block enablement)
- SPECIFICATION.md §13 ADR-026 (canonical DockerRunner architecture + ContainerFactory addendum)
- SPECIFICATION.md §13 ADR-027 (RawFinding.Metadata schema)
- SPECIFICATION.md §14.1 (asymmetric-cost meta-principle)
- SPECIFICATION.md §7.1 (job-payload wire schema; auth block)
- SPECIFICATION.md §9.1 (pricing tiers)
- SPECIFICATION.md §3.2 (architecture diagram)
- shieldscan-engine DEVELOPMENT-PATTERNS entry #5 (cleanup-uses-detached-context; applies to v2 warm-pool `CleanupFunc`)
- standing rule: all code/server work in single self-contained Copy-Paste Reply paragraph per phase
