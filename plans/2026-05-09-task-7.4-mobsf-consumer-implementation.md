# Task 7.4 MobSF Consumer (MAST) — Implementation Plan

> **For Claude:** This plan follows Task 7.3 phased implementation precedent (Phase 0 verification → Phase 1 framework primitives → Phase 2 cross-repo decision → Phase 3 testing infrastructure → Phase 4 atomic commit). Single atomic commit at Phase 4 covers all phases. Deviation discipline + phase-by-phase Copy-Paste Reply instructions under the project's standing rule.

**Goal:** Ship MobSF as second DockerServiceRunner consumer (per Task 7.5b framework; commit 1306ca8). Validates MAST (Mobile Application Security Testing) capability for ShieldScan v1; consumes existing mobile scaffolding (Task 3.X / 2.3.2 / 3.3 vintage); enables Mobile scans/mo pricing tier (Growth/Business/Enterprise). C5 typed-fields-first 3rd-instance promotion candidate at Phase 5.D.

**Architecture:** Per design doc `plans/2026-05-09-task-7.4-mobsf-consumer-design.md` (commit 02be8cf). 9 brainstorming decisions Q1–Q9 + Q5-first-ordering-per-Path-1 + 18 Phase 0 V-items + 6 Phase 0 pre-brainstorming deviations resolved. Path Y verification grounded Q4+Q7-partial+Q8 against MobSF source-code reading + Task 7.5c V4 verification precedent (commit bfccef8) directly applied to Q5 architectural decision.

**Tech Stack:** Go 1.26.2; Docker SDK v28.5.2+incompatible (already imported via Task 7.5a/b); AWS SDK Go v2 (NEW import for R2 S3-compatible client; Q6.1 consumer-local placement); zerolog; net/http stdlib (per Task 7.5b custom HTTP client framework); testify v1.11.1; existing service framework at `internal/tools/docker/service/` (Task 7.5b commit 1306ca8).

**Reference artifacts:**
- Design doc: `plans/2026-05-09-task-7.4-mobsf-consumer-design.md` (commit 02be8cf in shieldscan-docs)
- Framework: shieldscan-engine commit 1306ca8 (Task 7.5b DockerServiceRunner + ContainerFactory hook)
- Consumer pattern precedent: shieldscan-engine commit e905afe (Task 7.3 ZAP consumer); shieldscan-engine commit 872b2b0 (Task 7.2 Nmap consumer)
- WithAPIKeyHeader correction: shieldscan-engine commit 24c5f49 (Task 7.5b cleanup; MobSF is now canonical example for the framework helper)
- Q5 architectural precedent: shieldscan-engine commit bfccef8 (Task 7.5c V4 verification empirical findings)

---

## Phase 0 — Empirical Verification (READ-WRITE WITH CONTAINERS; ~90-120min; NO COMMIT)

MobSF-container-empirical verification of 18 V-items surfaced by design doc Path Y verification rounds + Phase 0 read-only verification surface. Most V-items require MobSF container spinup at pinned digest (derived from `opensecurity/mobile-security-framework-mobsf:v4.4.6`); some require sample APK/IPA testbed for empirical response shape capture.

### Phase 0 Status

**Phase 0 Status (2026-05-09):** PENDING. Phase 0 of Task 7.3 precedent (engine commit e905afe) caught V0 NEW infrastructure finding + V5/V17 + V6 + V8 + V12 drift corrections during execution; Task 7.4 Phase 0 follows same empirical discipline.

### V1–V18 Verification Items (per design doc §3.5)

- **V1.** Verify pinned MobSF digest resolves correctly + multi-arch. Run: `docker pull opensecurity/mobile-security-framework-mobsf:v4.4.6`; `docker manifest inspect <image>`; capture digest `sha256:<...>`; verify amd64 + arm64. Pin digest for subsequent V-items.
- **V2.** Verify `MOBSF_API_KEY` env var startup behavior. Spin up: `docker run --rm -d --name mobsf-verify -p 18000:8000 -e MOBSF_API_KEY=<test-key> <pinned-digest>`. Inspect logs for daemon ready + auth-required behavior. Curl `/api/v1/ping` with + without API key; confirm 401 on missing key.
- **V3.** `WithAPIKeyHeader("X-Mobsf-Api-Key", key)` empirical confirmation. `curl -H "X-Mobsf-Api-Key: <test-key>" http://localhost:18000/api/v1/scorecard?hash=fake`; capture response status; verify `Authorization` fallback header also works.
- **V4.** `/api/v1/scorecard` + `/api/v1/report_json` response shape against v4.4.6 reality. First populate via test APK upload (V11 dependency). Then curl scorecard + report_json with hash. Capture response JSON; compare against design doc §3.4 mapping table; document drift.
- **V5.** Severity enum strings empirical (`ERROR`/`WARNING`/`INFO` uppercase per source-code reading; verify in actual `report_json`).
- **V6.** `metadata.cwe` format empirical (`"CWE-89: SQL Injection"` canonical per source-code reading; verify variants OR confirm canonical).
- **V7.** `metadata.owasp-mobile` + `metadata.masvs` format strings empirical.
- **V8.** `rule_id` human-readability decision — sample 5–10 rule_id values from test scan; assess whether rule_id suitable for `Title` OR `metadata.description`-first-sentence better.
- **V9.** `report_json` response size for typical APK + larger APK. Use test APKs of varying sizes; capture `report_json` size per scan; ADR-017 Option C surveillance threshold (10MB?).
- **V10.** Android vs iOS section-name empirical. Run scans against test APK + test IPA; capture section names; document Android (`manifest_analysis`) vs iOS (`info_plist_analysis` OR similar) variance.
- **V11.** `manifest_analysis` section per-item shape (Android).
- **V12.** `permissions` section per-item shape.
- **V13.** `network_security` section per-item shape.
- **V14.** `binary_analysis` + `macho_analysis` section per-item shapes.
- **V15.** `secrets` section per-item shape.
- **V16.** `trackers` section per-item shape.
- **V17.** `certificate_analysis` section per-item shape.
- **V18.** `/api/v1/delete_scan` response shape (Task 7.5d preparation; not Phase 1 v1 scope).

### Phase 0 Sample APK + IPA Testbed

Task 7.5c precedent: juice-shop testbed used for ZAP. Task 7.4 needs APK + IPA samples. Candidates:
- **DIVA** (Damn Insecure and Vulnerable App; Android; ~5MB; widely-used MobSF testbed)
- **InsecureBankv2** (Android; ~10MB)
- **iGoat-Swift** (iOS; ~50MB; OWASP iGoat project)

Recommend DIVA + iGoat-Swift for Android + iOS coverage; sample binaries downloadable from project GitHub releases. Pin SHA256 of testbed binaries at execution time.

### Phase 0 STOP GATE

- **V4** (`report_json` schema drift): if v4.4.6 response shape diverges substantially from source-code reading (e.g., section names renamed; per-rule object restructured; metadata fields removed), surface STOP GATE for design doc revision before Phase 1.
- **V18** (`delete_scan` response): not blocking Phase 1 (ephemeral path doesn't use); audit-only finding.

Phase 0 produces NO commit. Surface report enables Phase 1+ design grounding.

---

## Phase 1 — MobSF Consumer Subpackage (~3-4h; NO COMMIT)

### Files Created

Files created/modified in Phase 1 (per design doc §4):

**MobSF consumer subpackage** (net-new at `internal/tools/docker/service/mobsf/`):
- `mobsf.go` — Config struct + NewBuildScan constructor + lifecycle composition; ~180 LoC
- `r2.go` — consumer-local R2 S3-compatible client via AWS SDK Go v2; binary download to worker-local `/tmp` staging; ~120 LoC
- `upload.go` — MobSF `/api/v1/upload` multipart wrapper + hash response parsing; ~80 LoC
- `scan.go` — MobSF `/api/v1/scan` synchronous invocation + ctx-derived timeout; ~70 LoC
- `parser.go` — `report_json` single-pass per-section parser + Q8 mapping table + mobile-evidence typed-field population + Severity normalization + CWE numeric extraction; ~280 LoC
- `sections.go` — per-section adaptors (`code_analysis` primary + 7 secondary sections; pluggable section handler pattern); ~150 LoC
- `target.go` — target validation (`TargetType=mobile` + `MobileUploadRef` + `Platform∈{android,ios}` + `AnalysisType=static` v1 + file-extension validation per Q2(iv)); ~60 LoC

**Test files:**
- `mobsf_test.go` (~150 LoC)
- `r2_test.go` (~120 LoC; httptest-stubbed S3-compatible responses)
- `upload_test.go` (~80 LoC)
- `scan_test.go` (~60 LoC)
- `parser_test.go` (~300 LoC; per-section coverage; Q8 mapping correctness; CWE extraction; Severity normalization with drop semantics)
- `sections_test.go` (~150 LoC)
- `target_test.go` (~80 LoC)
- `testhelpers_test.go` (~180 LoC; stub MobSF responder using `httptest.NewServer` with canned `report_json` + scorecard + upload + scan responses per V4 verification)

**Cross-package modifications (Q3 lock only):**
- `deploy/docker-compose.services.yml` — remove MobSF service entry (~13 LoC removed)
- `deploy/docker_compose_test.go` — drop MobSF assertions at lines 50 + 71 (~10 LoC removed)

**NO Q6 cross-repo work** (Phase 2 OUTCOME b locked pre-brainstorming per V6 finding); **NO ScanConfig field additions** (mobile-config wire schema pre-existed per V1 finding); **NO new framework primitives** (Task 7.5b framework sufficient per Q5 ephemeral lock + Q7 `WithAPIKeyHeader` directly used).

**Total estimated:** ~940 LoC production + ~1120 LoC tests + ~23 LoC cross-package removals. Net delta: **~+2037 LoC engine-side**.

### Phase 1 Quality Gate

- gofmt clean
- go vet clean
- golangci-lint clean
- all Phase 1 tests pass under `go test -race -count=1 ./...`
- existing `internal/tools/docker/service` tests still pass (Task 7.5b backward-compat)
- Task 7.3 ZAP consumer tests still pass (Task 7.3 backward-compat per e905afe)
- existing 23 packages green (post-Task-7.3); new `internal/tools/docker/service/mobsf` package green (24 total)
- `deploy/docker_compose_test.go` test set updated coherently

**PAUSE AFTER PHASE 1.**

---

## Phase 2 — Cross-Repo Verification + Disposition (~15-25min; READ-ONLY; NO COMMIT)

Q6 lock locked Phase 2 **OUTCOME b** pre-brainstorming per V6 finding: shieldscan-api comprehensive mobile infrastructure already shipped (MobileUpload SQLAlchemy + MobileUploadResponse Pydantic + R2 service + mobile route + 2 migrations + service + tests; Task 3.X / 2.3.2 / 3.3 vintage). NO Phase 2 cross-repo modifications expected.

**Phase 2 verification (read-only confirmation):**

```bash
cd /home/mahmoud/workspace/shieldscan-api 2>/dev/null
grep -rln "MobileUpload\|mobile_upload\|MOBSF\|mobsf" --include="*.py" . \
  | grep -v __pycache__ | grep -v ".git" | head -15
cat src/app/models/mobile.py | head -40
grep "mobile_config\|JobMobileConfig" src/app/services/orchestrator.py
```

(verify shieldscan-api mobile infrastructure intact and unchanged; re-confirm `MobileUpload` SQLAlchemy shape per V6 finding; re-confirm orchestrator emits `mobile_config` block).

**Phase 2 disposition: OUTCOME b** — engine Phase 4 commit ships standalone. ZERO shieldscan-api files modified. Cross-repo coordination already complete from Task 3.X vintage.

**Forward-pin:** pre-signed URL pattern for R2 binary fetch is ADR-015 enablement task scope; not Phase 2 Task 7.4 scope. R2 credentials in engine via env vars v1 per Q6.4 lock.

**PAUSE AFTER PHASE 2.** Surface findings confirming OUTCOME b lock holds (no unexpected partial work surfaces; mobile infrastructure intact).

---

## Phase 3 — Testing Infrastructure + Documentation (~45-60min; NO COMMIT)

Coverage report for `service/mobsf` subpackage (target ≥80% per Task 7.3 precedent); race detector clean; godoc verification for new exported types (`Config`, `NewBuildScan`, `R2Config`, `MobSFTimeout`). Package-level documentation in `mobsf.go` top-of-file; consumer-integration docstring on Config struct mirroring Task 7.5b service.go pattern + Task 7.3 zap.go precedent.

### Phase 3 Quality Gate

- Coverage ≥80% on `service/mobsf` package
- race detector clean
- godoc renders correctly
- lint clean

**PAUSE AFTER PHASE 3.**

---

## Phase 4 — DRIFT-LOG + Atomic Commit (~30-45min; SINGLE COMMIT)

Land all Phase 1+3 work in single atomic commit per Task 7.5a + Task 7.5b + Task 7.3 precedent.

### Pre-Commit Steps

1. Update `DRIFT-LOG.md` with Phase 1+3 deviation enumeration following Task 7.3 commit e905afe pattern (Task 7.4 section above Task 7.5c entry per chronological newest-first placement).
2. Final quality gate: gofmt + go vet + golangci-lint + `go test -race -count=1 ./...`.
3. Verify staged file set: MobSF subpackage (production + test) + cross-package modifications (docker-compose) + DRIFT-LOG + commit message.
4. No shieldscan-api state changes per Phase 2 OUTCOME b.

### Commit Message Shape

Mirroring e905afe + 1306ca8 + 872b2b0 + f5d77c8 commit message conventions; documents architecture per design doc 02be8cf; enumerates V1–V18 + 6 Phase-0 pre-brainstorming deviations + Q5-first ordering rationale with disposition; forward-pins Task 7.5d empirical verification + ADR-008 addendum at Phase 5.B + C5 promotion at Phase 5.D + ADR-015 enablement scope expansion + framework-level R2 client at 2nd-instance threshold.

**PAUSE AFTER PHASE 4.** Task 7.4 engine-side closed.

---

## Forward-Pins After Task 7.4 Closes

- **Task 7.5d — MobSF `delete_scan` cleanup contract empirical verification.** Plan-then-execute pattern per Task 7.5c 5a253d2 precedent. Decision criteria: surfaces analogous to Task 7.5c S7/S8/S9 (Suppression registry; user accounts; instance settings). If verification surfaces COMPLETE, flip `cfg.EphemeralContainer` to `false` + design `CleanupFunc` using `delete_scan` + analogous wrappers. ~3-4h dedicated session.
- **ADR-008 addendum at Phase 5.B Task 7.4** — acknowledges V4 + V5 cleanup-verification deferral pattern; refines architectural commitment from "persistent service" to "persistent-service-with-verified-cleanup OR ephemeral-fallback-when-cleanup-UNCLEAR." Mirrors Task 7.5b 5.B ADR-026 addendum commit 066c81f.
- **C5 typed-fields-first promotion at Phase 5.D Task 7.4** — 3-instance threshold reached (Nmap + ZAP + MobSF); DEVELOPMENT-PATTERNS.md entry #6 promotion candidate. Mirrors Task 7.2 Phase 5.C entry #5 commit 5503476 pattern.
- **Task 7.4 Phase 5.A design doc revision** — Phase 0 V1–V18 empirical drift annotation per Task 7.3 5.A pattern (commit 26e9afa).
- **ADR-015 enablement task scope expansion** — R2 pre-signed URL pattern for binary fetch (Q6.4 forward-pin). Bundles with auth credentials transit work; orchestrator decryption + URL generation; lifts engine-env-var dependency.
- **Mode D dynamic analysis v2** — when Android emulator infrastructure surfaces.
- **Mode source code analysis v2** — when customer demand for source-archive analysis surfaces.
- **ADR-017 Option C surveillance** — MobSF `report_json` typical 100KB–5MB; large APKs may exceed 10MB; surveillance at Phase 0 V9 outcome.
- **Framework-level R2 client promotion** — when 2nd consumer requires R2/S3 binary fetch; 3-instance threshold per Phase 5.D pattern.
- **Async-mode MobSF (`MOBSF_ASYNC_ANALYSIS=1`)** — v1 ships sync; flip when scan duration exceeds framework client timeout.
- **Task 7.1 (Trivy) + Task 7.6 (SQLMap)** — DockerRunner exec-shape consumers; per-Task-7.2 precedent.

---

## Discipline Pattern Carry-Forward

The verify-then-adapt-then-document discipline established by Task 7.5a + Task 7.2 + Task 7.5b + Task 7.3 + Task 7.5c carries forward. Anticipated deviation surfaces:

- Phase 0 V1–V18 will surface 5–10 factual deviations (auto-corrected)
- Phase 1 MobSF parser may surface `report_json` shape edge cases (`rule_id` format; per-section per-item shape variance; iOS section-name differences from Android)
- Phase 1 R2 client may surface AWS SDK Go v2 import + initialization patterns

Pre-implementation guidance is unreliable specifically because edge-case interleavings + cross-repo wire deserialization + per-section parsing surface during implementation; the discipline pattern catches them transparently. Phase 4 commit message footnote enumerates deviations explicitly.

---

## Cross-References

- Design doc: `plans/2026-05-09-task-7.4-mobsf-consumer-design.md` (commit 02be8cf in shieldscan-docs)
- Framework foundation: shieldscan-engine commit 1306ca8 (Task 7.5b DockerServiceRunner + ContainerFactory hook)
- Consumer pattern precedent: shieldscan-engine commit e905afe (Task 7.3 ZAP consumer; `WithAPIKeyHeader` correction at 24c5f49 makes MobSF canonical example); shieldscan-engine commit 872b2b0 (Task 7.2 Nmap consumer)
- Q5 architectural precedent: shieldscan-engine commit bfccef8 (Task 7.5c V4 verification empirical findings)
- Brainstorming chain artifact: in conversation memory + design doc 02be8cf §2 + §3.5
- SPECIFICATION.md §13 ADR-008 (MobSF persistent service architectural commitment; Phase 5.B addendum target)
- SPECIFICATION.md §13 ADR-015 (decrypted credentials in Redis transit; forcing function for R2 pre-signed URL)
- SPECIFICATION.md §13 ADR-017 (Option C report-size surveillance)
- SPECIFICATION.md §13 ADR-026 (canonical DockerRunner architecture + ContainerFactory addendum)
- SPECIFICATION.md §13 ADR-027 (RawFinding.Metadata schema)
- SPECIFICATION.md §14.1 (asymmetric-cost meta-principle; Q5 invocation rationale)
- SPECIFICATION.md §7.1 (mobile_config wire schema; pre-existing)
- SPECIFICATION.md §9.1 (Mobile scans/mo pricing tier) + §9.2 (Mobile scan pack add-on)
- SPECIFICATION.md §3.2 (architecture diagram; MobSF subpackage path target at Phase 5.C)
- shieldscan-engine DEVELOPMENT-PATTERNS entry #5 (cleanup-uses-detached-context; precedent for C5 promotion at Phase 5.D)
- standing rule: all code/server work in single self-contained Copy-Paste Reply paragraph per phase
